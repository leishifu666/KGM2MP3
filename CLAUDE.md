# OneClick-KugouMP3 项目开发约束

## 项目性质
- 这是一个 **Windows PowerShell + WinForms** 的本地桌面工具项目。
- 主入口是 `启动转换器.bat` 和 `app.ps1`。
- 当前真实主流程在 `app.ps1`，不是 Node / Web 项目。
- `ui/` 目录不是当前主入口，除非用户明确要求，否则优先修改 `app.ps1`。

## 目录职责
- `tools/`：第三方工具目录，只允许**新增**明确来源和版本的工具，不随意替换已有 `exe / dll / mask`。
- `input/`：输入文件目录，属于运行数据，不作为常规源码修改目标。
- `output/`：输出产物目录，不作为常规源码修改目标。
- `logs/`：日志目录，不作为常规源码修改目标。
- `work-*`：临时工作目录，不作为常规源码修改目标。

## 开发规则
- 优先保持单文件结构，不做无必要的大拆分。
- 新增格式支持时，优先复用 `Convert-AudioFiles` 现有流水线。
- MP3 输出统一通过 `ffmpeg.exe` 处理。
- 所有新格式支持都必须带有“缺工具时可理解提示”。
- 用户界面和提示文案面向小白，优先简单直接，不堆术语。
- 修改 `app.ps1` 时，优先做增量修改，不重构整套流程。
- 不替换现有 `ffmpeg.exe`、`unlockKuGoWin-64.exe`、`kgg-dec.exe`、`infra.dll`，除非用户明确要求。

## Git 规则
- 提交信息默认使用中文。
- 没有用户明确允许，禁止自行执行 git 提交。

## 验证规则
- 修改 `app.ps1` 后，必须做 PowerShell 语法检查。
- 涉及转换流程的修改，必须至少做一次真实样本链路验证。
- 增加新格式或新参数后，必须回归现有 `.kgm` / `.kgma` / `.kgg` / `.flac` 流程，避免带坏旧能力。

## 当前规划约定
- 输出质量/码率只做 3 档：高质量（默认）、标准、省空间。
- `.ncm` 支持优先采用适合 Windows 本地打包的现成工具，优先考虑 `taurusxin/ncmdump`。
